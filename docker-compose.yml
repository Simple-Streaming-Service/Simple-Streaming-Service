services:
    web-app:
        environment:
            env_file: .env
        build: .
        command: ./boot.sh
    nginx:
        image: nginx:alpine
        ports:
            - "8088:8088"
        depends_on:
            - web-app
        volumes:
            - ./nginx.conf:/etc/nginx/nginx.conf